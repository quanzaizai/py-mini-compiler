# 📘 Mini-C 递归下降编译器：零基础工程架构与编译全链路全景指南

> **所属专业课**：《编译原理》《形式语言与自动机》《计算机系统全链路贯通》  
> **核心目标**：彻底打破对高级编程语言编译器的神秘感，掌握**词法分析（Lexer 字符流转 Token）、递归下降语法分析（Parser 构建 AST 抽象语法树）、文法优先级分层、代码生成器（CodeGen 翻译目标汇编）以及与底层虚拟 CPU 的端到端闭环执行**。

---

## 目录
1. [第一部分：工程规范、工具链与环境大解密](#第一部分工程规范工具链与环境大解密)
   * [1. 经典编译器四阶段流水线目录结构](#1-经典-编译器四阶段流水线目录结构)
   * [2. 跨模块软硬件全链路联调架构设计](#2-跨模块软硬件全链路联调架构设计)
   * [3. 自动化闭环测试套件设计](#3-自动化闭环测试套件设计)
2. [第二部分：核心原理、技术选型与编译理论全景](#第二部分核心原理技术选型与编译理论全景)
   * [1. 现实痛点与业务价值：为什么编译原理是程序员的封顶神技？](#1-现实痛点与业务价值为什么编译原理是程序员的封顶神技)
   * [2. 核心编译架构选型矩阵（Why this, not that?）](#2-核心编译架构选型矩阵why-this-not-that)
   * [3. 扫清“黑魔法”：运算符优先级文法分层与标签生成推导](#3-扫清黑魔法运算符优先级文法分层与标签生成推导)
   * [4. 全景 ASCII 从 C 源代码到芯片硅片执行的全链路图解](#4-全景-ascii-从-c-源代码到芯片硅片执行的全链路图解)
   * [5. 零基础手写复现通关路线卡（五步通关）](#5-零基础手写复现通关路线卡五步通关)

---

## 第一部分：工程规范、工具链与环境大解密

### 1. 经典编译器四阶段流水线目录结构

```text
py-mini-compiler/
├── ARCHITECTURE_AND_DESIGN.md  # 🌟 零基础编译原理与全链路手写指南
├── src/
│   ├── lexer.py                # 🔍 阶段一：词法分析器 (字符文本流 -> 结构化 Token 序列)
│   ├── ast_nodes.py            # 🌳 数据建模：抽象语法树节点 (Program, While, If, BinaryOp)
│   ├── parser.py               # 📐 阶段二：递归下降语法分析器 (Token 序列 -> 语法树 AST)
│   ├── codegen.py              # ⚙️ 阶段三：汇编代码生成器 (遍历 AST -> 生成 ToyCPU 汇编)
│   └── compiler.py             # 🚀 阶段四：总线驱动管线 (串联前端、后端与虚拟机全链路)
├── tests/
│   └── test_compiler.py        # 🧪 软硬件闭环全自动化测试
├── examples/                   # 💡 类 C 源码示例 (如 while 循环 1~100 累加)
├── pyproject.toml              # ⚙️ 现代标准项目元数据配置文件
├── .gitignore                  # Git 忽略配置
└── README.md                   # 项目导航说明手册
```

#### 为什么编译器的每个文件对应一个阶段？
* **严格单向数据流流水线（Pipeline Architecture）**：
  * 阶段 1（`lexer.py`）：输入 `char*` 文本，输出 `List[Token]`；
  * 阶段 2（`parser.py`）：输入 `List[Token]`，输出 `AST` 树根节点；
  * 阶段 3（`codegen.py`）：输入 `AST` 树，输出 `ToyCPU` 汇编字符串；
  * 每一层输出都是下一层的纯输入，没有任何循环依赖，职责纯粹。

---

### 2. 跨模块软硬件全链路联调架构设计

在 `src/compiler.py` 中，有一段极其优美的跨项目联动逻辑：
```python
# 动态将相邻的 toy-cpu-vm 引入路径
vm_dir = Path(__file__).resolve().parent.parent.parent / "04_architecture-and-compilers" / "toy-cpu-vm"
sys.path.append(str(vm_dir))
from assembler import Assembler
from cpu import CPU
```
* **绝妙体验**：你在 `py-mini-compiler` 里写的类似 C 语言代码，被编译器翻译为汇编，再由 `assembler.py` 汇编为字节码，最后注入给 `cpu.py` 虚拟芯片运行，屏幕上直接打印出物理结果！**全流程 100% 闭环无外部依赖！**

---

### 3. 自动化闭环测试套件设计

在 `test_compiler.py` 中：
* **测试 1（算术表达式）**：编译 `x = 15; y = 25; z = x + y * 2; print(z);`，验证乘法优先级与打印输出 `65`；
* **测试 2（While 循环闭环）**：编译经典的 `1` 累加到 `100` 循环，断言最终虚拟 CPU 输出 `5050`！
* **测试 3（If-Else 分支）**：编译条件跳转，断言正确的分支逻辑。

---

## 第二部分：核心原理、技术选型与编译理论全景

### 1. 现实痛点与业务价值：为什么编译原理是程序员的封顶神技？

很多工程师写了 10 年业务代码，依然把编译器当成黑盒：
* **业务痛点**：遇到语法报错只知道乱改，看不懂 Babel / SWC 是怎么把现代 JS 降级为 ES5 的，不知道代码静态检查工具（ESLint、静态类型推断）是如何扫描 AST 树的。
* **掌握编译器的收益**：
  * 你不再是“语言的奴隶”，而是**“规则的制定者”**；
  * 能手写 DSL（领域特定语言），为公司自研规则引擎、SQL 查询优化器或轻量脚本语言。

---

### 2. 核心编译架构选型矩阵（Why this, not that?）

#### 决策一：语法分析为什么选手写“递归下降法（Recursive Descent）”？

| 语法分析方法 | 原理 | 优缺点 | 工业界评价与本项目选型 |
| :--- | :--- | :--- | :--- |
| **生成器工具法 (Yacc / Bison / ANTLR)** | 编写文法规则文件，由工具自动生成数千行机械代码。 | ❌ 难以阅读和调试，报错信息反人类（经典的“语法错误：在第 12 行”）。 | 教学初期不建议 |
| **手写递归下降 (Recursive Descent - 本项目)** | **每一个文法产生式直接对应一个 Python 递归函数**。 |  **自解释性极强，报错信息精准友好**。GCC、Clang、Rust 官方编译器前端**全部采用手写递归下降**！ | **编译器世界最佳典范** |

---

### 3. 扫清“黑魔法”：运算符优先级文法分层与标签生成推导

#### 1. 乘法怎么做到天然优先于加法？（文法分层秘诀）
在数学中，`1 + 2 * 3` 必须先算 `2 * 3`。语法分析器是如何做到这一点的？
* **答案：文法分层（Grammar Layering）！**
  ```text
  层级 1 (最低优先级: + -): expr   -> term ((PLUS | MINUS) term)*
  层级 2 (中等优先级: * /): term   -> factor ((MUL | DIV) factor)*
  层级 3 (最高优先级: 括号/数): factor -> NUMBER | IDENTIFIER | LPAREN expr RPAREN
  ```
* **推演过程**：
  1. `parse_expr()` 进门先调用 `parse_term()`；
  2. `parse_term()` 进门先调用 `parse_factor()` 拿到数字 `1`；
  3. 此时发现后面是 `*`，`parse_term()` 内部在底层直接就把乘法节点 `2 * 3` 拼成子树！
  4. 外层的加法函数最后才把 `1` 和这个乘法子树连接起来！**优先级在文法递归调用栈深浅中自然天成！**

#### 2. 条件跳转汇编标签动态生成（Label Allocation）
在编译 `if (x > 5) { ... } else { ... }` 时，编译器如何生成汇编跳转点？
* **计数器分配法**：
  * 维护一个全局自增整数 `label_id`；
  * 每次遇到 if 语句，分配两个唯一标签：`IF_ELSE_{id}` 与 `IF_END_{id}`；
  * 生成比较指令：`CMP R0, #5; JLE IF_ELSE_1`，保证任何嵌套循环都不会标签重名！

---

### 4. 全景 ASCII 从 C 源代码到芯片硅片执行的全链路图解

```text
 1. 编写 Mini-C 源代码 (如 while 累加 1 到 100)
       │
       ▼
 2. lexer.py (字符扫描) ──► 输出 Token 流: [WHILE, LPAREN, IDENT("i"), LE, ...]
       │
       ▼
 3. parser.py (递归下降) ─► 构建出高层 AST 抽象语法树
       │
       ▼
 4. codegen.py (遍历 AST) ─► 生成 ToyCPU 汇编文本 (.asm)
       │
       ▼
 5. toy-cpu-vm / assembler.py (两遍扫描) ──► 翻译为 16 进制机器字节码 (.bin)
       │
       ▼
 6. toy-cpu-vm / cpu.py (硬件时钟 Fetch-Decode-Execute 周期)
       │
       ▼
 🖥️ 终端屏幕输出: R0 = 5050 (🎉 纯手工自制芯片与编译器胜利会师！)
```

---

### 5. 零基础手写复现通关路线卡（五步通关）

当你准备自己从零实现这个微型编译器时，按照以下 5 个关卡递进：

```text
关卡 1：构建词法分析器与 Token 定义 (lexer.py)
  ├── 目标：实现 advance() 与 peek()，按正则提取关键字 (while/if/print)、数字与符号。
  └── 验证：输入 "int a = 10;"，能够准确输出 [KEYWORD_INT, IDENT("a"), ASSIGN, NUM(10), SEMI]。

关卡 2：定义 AST 语法树节点与基础四则运算解析 (ast_nodes.py + parser.py)
  ├── 目标：根据文法分层实现 parse_expr(), parse_term(), parse_factor()。
  └── 核心：支持加减乘除与括号嵌套，生成二叉语法树。

关卡 3：语法分析器支持变量声明、赋值与打印语句
  ├── 目标：实现 parse_statement() 与 parse_program()。
  └── 验证：能够正确解析完整的赋值与打印语句块。

关卡 4：支持 If-Else 与 While 循环控制流分支
  ├── 目标：解析带大括号 {} 的复合语句块。
  └── 核心：将条件表达式与主体语句组装为 WhileStmt 和 IfStmt 树节点。

关卡 5：汇编代码生成与软硬件端到端联调 (大圆满收官)
  ├── 目标：编写 codegen.py，遍历 AST 生成 ToyCPU 汇编代码，并调用虚拟机执行。
  └── 验证：运行 python3 tests/test_compiler.py，1 到 100 累加输出 5050，全绿 PASS！
```
